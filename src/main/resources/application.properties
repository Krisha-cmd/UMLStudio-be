spring.application.name=backend

# H2 in-memory datasource for development and tests
# spring.datasource.url=jdbc:h2:mem:umlstudio;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=

# # JPA / Hibernate
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true

# # Enable H2 console
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console


spring.datasource.url=jdbc:postgresql://localhost:5432/umlstudio
spring.datasource.username=postgres
spring.datasource.password=Aditya

#optional
spring.datasource.driver-class-name=org.postgresql.Driver


# JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect



# JWT configuration
# NOTE: In production use a securely stored secret (environment variable or secrets manager).
jwt.secret=changeitchangethissecretkeythatshouldbelonger
jwt.expiration-ms=3600000


# spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration


# MySQL configuration (example) - commented out for now. When switching to MySQL:
# 1) Add MySQL URL, username and password below (uncomment and edit values).
# 2) Change spring.jpa.database-platform to the appropriate MySQL dialect (e.g., org.hibernate.dialect.MySQL8Dialect).
# 3) Ensure the MySQL connector dependency is present in pom.xml (already included but runtime scope) and
#    configure connection pooling if needed.
#
# spring.datasource.url=jdbc:mysql://localhost:3306/umlstudio?useSSL=false&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=your_password_here
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
# spring.jpa.hibernate.ddl-auto=update
